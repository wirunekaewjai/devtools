export enum Builder {
  NODE = "node",
  VERCEL = "vercel",
}

export enum Command {
  BUILD = "build",
  DEV = "dev",
}

export enum Environment {
  DEVELOPMENT = "development",
  PRODUCTION = "production",
}
